package _d_chapter4._h_lambda;

public class LambdaPractice {

    public static void main(String[] args) {
       // LambdaPractice.test(()->true); //does not compile - a functional interface is expected as parameter instead of boolean
    }

    static void test(boolean b){

    }
}
