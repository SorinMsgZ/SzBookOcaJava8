package chapter1.classes;

public class MainDecompositionMode1 {
    public static void main(String[] args) {
        System.out.println(args[0]);
        System.out.println(args[1]);

        /*
        In the "Run/Debug Configurations" dialog, click on the "+" icon and select "Application" from the list.
        In the "Main class" field, enter the fully qualified name of the class containing the main method.
        In the "Program arguments" field, provide the command-line arguments separated by spaces.

         or

         from command prompt run "java MainDecomposition argumet1 argument2"
         */
    }
}
